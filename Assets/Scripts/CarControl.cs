using UnityEngine;
using System.Collections;

public class CarControl : MonoBehaviour {

	public float turnRadiusScalar;
	public float accelerationScalar;
	float currentThrust;

	void Update() {
//		if (Mathf.Abs(m_currThrust) > 0)
//			m_body.AddForce(transform.forward * m_currThrust);
	}
}
